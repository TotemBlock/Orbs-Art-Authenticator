{
  "name": "orbs-starter-kit",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "jquery-simple-upload": "^1.1.0",
    "js-sha256": "^0.9.0",
    "nodemon": "^1.19.3",
    "orbs-client-sdk": "1.0.0",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-scripts": "3.0.1",
    "sha256": "^0.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "eject": "react-scripts eject",
    "build": "react-scripts build",
    "watch": "nodemon --exec \"react-scripts build\" --watch src",
    "test": "react-scripts test",
    "lint": "prettier --write \"src/**/*.js\""
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "prettier": "^1.17.1"
  },
  "prettier": {
    "singleQuote": true
  }
}
